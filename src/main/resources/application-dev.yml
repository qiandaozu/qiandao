spring:
  profiles:
    active: dev

---

server:
  port: 8080

#spring:
#  datasource:
#    username: root
#    password: root
#    url: jdbc:mysql://localhost:3306/hehe02?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
#    driver-class-name: com.mysql.jdbc.Driver

#ali durid
spring:
  datasource:
    username: root
    password: Qiandao1..
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://39.105.179.42:3306/qiandao?serverTimezone=UTC&useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource
    #   数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: false
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    exceptionsorter: true
    filters: stat,wall
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500


mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: cn.qiandao.pojo

#showSql
logging:
  level:
    com:
      example:
        mapper : debug

---

# redis
spring:
  redis:
    host : 127.0.0.1
    port : 6379
    #Redis服务器连接密码（默认为空）
    #password:
    jedis:
      pool:
        #连接池最大连接数（如果配置<=0,则没有限制）
        max-active: 8
    #选择你使用的是Redis的第几个数据库
    database: 4

---

# RabbitMq
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    publisher-confirms: true   #消息发送到交换机确认机制，是否确认回调
    publisher-returns: true
    template:
      mandatory: true
  devtools:
    restart:
      enabled: false

---

